const style = (theme) => ({
    font: {
        fontSize: "20px",
        fontWeight: 600,
        letterSpacing: "0.5rem"
    },
    color: {
        color: theme.palette.primary.light
    }
   
})
export default style;